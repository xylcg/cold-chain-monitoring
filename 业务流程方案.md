# 冷链物流智能监控平台 — 完整业务流程方案

> 文档基于项目实际代码梳理（cold-chain-monitoring）
> 技术栈：FastAPI + Vue3 + TDengine + PostgreSQL + Redis + Kafka + PyTorch
> 版本：v1.0.0

---

## 一、系统定位

平台覆盖冷链全链路：**产地预冷 → 冷藏运输 → 冷仓存储 → 末端配送 → 客户签收**，
通过 IoT 传感器实时采集温湿度/位置/车门/振动等数据，结合 LSTM 深度学习模型做异常检测、
温度预测、冷机故障预测与生鲜品质 AI 评估，实现"全程可视、风险预警、溯源可查"。

---

## 二、角色体系（4 类）

| 角色 | 默认账号 | 权限范围 | 入口页面 |
|------|----------|----------|----------|
| **管理员 admin** | admin / 123456 | 所有 PC 端页面 + 全部 API | /dashboard |
| **仓管员 warehouse** | warehouse01 / 123456 | PC 端仓储相关页面（除纯 admin 页） | /warehouse |
| **司机 driver** | driver01 / 123456 | 司机移动端工作台 | /driver-app |
| **客户 customer** | customer01 / 123456 | 客户移动端配送查询 | /customer-app |

权限控制：前端路由守卫（router/index.ts）+ 后端 `require_role()` 依赖注入双重校验。
JWT token 有效期 8 小时，登录接口 `/api/v1/auth/login`。

> 注：开发模式下后端 `get_current_user` 在未带 token 时默认放行 admin，便于本地调试。

---

## 三、全链路主业务流程

```
┌─────────────┐   ┌──────────────┐   ┌─────────────┐   ┌──────────────┐   ┌────────────┐
│ 1.订单生成  │ → │ 2.资源调度   │ → │ 3.多温区派车 │ → │ 4.冷藏运输    │ → │ 5.入库/出库 │
│  客户/管理   │   │  仓库/资源   │   │  调度中心    │   │  实时追踪     │   │  品质评估  │
└─────────────┘   └──────────────┘   └─────────────┘   └──────────────┘   └────────────┘
       │                                                                          │
       │                                                                          ▼
       │                                                                  ┌──────────────┐
       └──────────────────────────────────────────────────────────────│ 6.全程溯源   │
                                                                          │  区块链存证  │
                                                                          └──────────────┘
```

### 阶段 1：订单生成与接入
- **客户移动端**（/customer-app）：客户下单，生成运单。
- **订单管理中心**（/manager-orders，warehouse 角色）：管理员/仓管查看、管理全部订单状态
  （待接单 → 已接单 → 配送中 → 已送达 → 已完成）。
- 数据接口：`/api/v1/customer/*`、`/api/v1/dispatch/orders`。

### 阶段 2：资源智能调度
- **资源调度页**（/resources）：分配冷库仓位、冷藏车辆、冷板等资源。
- 支持仓库入库/出库登记、库存查询、资源利用率与需求预测看板。
- 老板视角：经营财务分析、司机绩效、客户分析（`/boss` 页 + `/api/v1/resources/boss-*`）。
- 数据接口：`/api/v1/resources/*`（allocate-all / warehouse-inbound / warehouse-outbound / utilization / forecast）。

### 阶段 3：多温区调度与派车
- **多温区调度页**（/dispatch）：根据货物温区要求（冷冻/冷藏/常温）自动匹配车厢温区，
  生成派车计划，司机在移动端确认接单。
- 数据接口：`/api/v1/dispatch/*`（assign / plan / confirm / monitor / cargo-zone-map）。

### 阶段 4：冷藏运输 — 实时监控
- **司机移动端**（/driver-app，本次重点修改页面）：
  - 实时监控：车厢温度/湿度/冷机状态、车门状态、车辆位置。
  - 配送进度：路线站点、时效追踪。
  - 异常预警：接收告警并处置（拍照/备注闭环）。
  - 凭证上传：运输温度记录纸拍照留存。
  - 运单管理：订单列表与详情。
  - **"我的"页**：用户信息 + 退出登录（本次新增）。
- **车辆追踪页**（/tracking，PC）：地图实时位置、轨迹回放。
- **路径规划页**（/routes）：智能规划配送路线，支持重规划。
- **电子围栏页**（/geofence）：划定合规区域，偏离/超时触发围栏告警。
- 数据接口：`/api/v1/driver/*`、`/api/v1/vehicles/*`、`/api/v1/routes/*`、`/api/v1/geofence/*`。

### 阶段 5：入库 / 出库品质评估
- **品质评估页**（/quality，本次修改页面）：
  - 场景切换：**入库质检** / **出库复核**（切换时自动清空上次结果）。
  - 拍照或本地上传生鲜图片 → AI 评估品质等级（A/B/C/D）+ 保鲜期建议 + 放行决策。
  - 支持品类：水果/蔬菜/肉类/海鲜/乳制品/豆制品/蛋类/医药制品/花卉。
- 数据接口：`/api/v1/quality/assess`、`/assess/image`、`/demo`、`/categories`。

### 阶段 6：全程溯源与区块链存证
- **追溯页**（/traceability，PC） + **客户公开查询**（`/public/trace/:code`）：
  - 消费者扫码查看全链路温控曲线、各节点时间、合规状态。
  - 区块链存证校验：`/api/v1/traceability/blockchain/verify/{code}`。
- 数据接口：`/api/v1/traceability/*`（auto/sensor、auto/dispatch、auto/resource、auto/geofence 自动写入溯源链）。

---

## 四、智能预警与应急处置流程

```
传感器数据 → 告警引擎(alert_engine) → 等级判定(normal/severe/critical)
     │                                    │
     │                                    ├─ 严重/紧急 → Kafka 推送 → WebSocket 实时告警
     │                                    └─ 生成处置预案 → 司机/仓管闭环处理
     ▼
告警中心(/alerts) → 确认(acknowledge) / 处置(handle) / 解决(resolve)
                              │
                              └─ 应急处置预案：/emergency/plans → trigger → step 按步执行
```

- 告警规则可配置（/rules 页 + `/api/v1/alerts/rules`）。
- 温度阈值统一在 `schemas/__init__.py` 的 `TEMP_THRESHOLD` 定义（达标 -25~8℃，预警上限 8℃，严重 15℃）。
- 数据接口：`/api/v1/alerts/*`（active / level/{normal|severe|critical} / driver / acknowledge / resolve / emergency/*）。

---

## 五、AI 能力矩阵

| 能力 | 模型 | 用途 | 接口/页面 |
|------|------|------|-----------|
| 温度异常检测 | LSTM 自编码器 | 实时识别温度越限/骤变 | alert_engine + /alerts |
| 温度趋势预测 | LSTM/Transformer | 预测未来 30min 风险 | /api/v1/temperature/trend、/maintenance |
| 冷机故障预测 | 预测性维护模型 | 提前预警冷机故障 | /maintenance 页 |
| 生鲜品质评估 | 图像识别 + 规则 | 入库/出库品质分级 | /quality 页 + /api/v1/quality/assess |

模型文件：`models/lstm_anomaly_detector.py`、`models/lstm_temperature_predictor.py`。

---

## 六、数据架构与实时链路

```
传感器/模拟器 ──POST /api/v1/sensors/data──┐
                                            ├─→ TDengine(时序存储)
                                            ├─→ Redis(实时状态/缓存, 含内存fallback)
                                            └─→ Kafka(coldchain_sensor_data)
                                                        │
                                                  消费者/模型服务
                                                        │
                                              ┌─────────┴─────────┐
                                         Kafka(alerts)      Kafka(predictions)
                                              │                  │
                                         WebSocket告警推送    前端HTTP轮询拉取
                                        (/ws/alerts 等)
```

- **TDengine**：传感器时序数据、告警事件、温度预测结果（端口 6041，不可达时降级模拟模式）。
- **PostgreSQL**：业务关系数据（订单/用户/资源，端口 5432）。
- **Redis**：设备在线状态、最新数据、温度滑动窗口、告警冷却（端口 6379，不可达时切内存存储）。
- **Kafka**：传感器/告警/预测消息流（端口 9092）。
- **WebSocket**：后端已提供 3 通道（`/ws/device/{id}`、`/ws/alerts`、`/ws/dashboard`），
  **当前前端通过 HTTP 轮询拉取数据，WebSocket 实时通道尚未在前端接入**（待完善点）。

---

## 七、PC 端功能模块总览（admin/warehouse）

| 页面路由 | 功能 | 核心 API |
|----------|------|----------|
| /dashboard | 全局态势大屏 | /dashboard/kpi、/devices、/overview |
| /boss | 经营分析看板（admin） | /resources/boss-* |
| /warehouse | 仓库管理工作台 | 综合 |
| /monitor | 设备监控 | /sensors/* |
| /temperature | 温度趋势 | /temperature/* |
| /alerts | 告警中心 | /alerts/* |
| /rules | 告警规则配置 | /alerts/rules |
| /geofence | 电子围栏 | /geofence/* |
| /traceability | 冷链追溯 | /traceability/* |
| /customer | 客户查询 | /customer/query |
| /tracking | 车辆追踪 | /vehicles/* |
| /routes | 路径规划 | /routes/* |
| /dispatch | 多温区调度 | /dispatch/* |
| /maintenance | 故障预测 | /maintenance/* |
| /quality | 品质评估 | /quality/* |
| /resources | 资源调度 | /resources/* |
| /manager-orders | 订单管理 | /dispatch/orders、/customer/* |

---

## 八、移动端功能模块

| 端 | 路由 | 功能 |
|----|------|------|
| 司机端 | /driver-app | 实时监控、配送进度、异常预警、凭证上传、运单管理、**我的(退出登录)** |
| 客户端 | /customer-app | 订单查询、配送追踪、温控曲线 |
| 公开监控 | /mobile | 移动端冷链监控（无需登录） |
| 公开溯源 | /public/trace/:code | 消费者扫码溯源 |

---

## 九、待完善 / 优化建议

1. **前端 WebSocket 接入**：后端已具备实时推送能力，但前端仍用轮询，建议接入 `/ws/*` 提升实时性。
2. **认证安全**：当前为开发模式（明文密码 123456 + 默认 admin 放行），生产需启用 bcrypt 校验与严格鉴权。
3. **TDengine/Redis 降级**：当前依赖 fallback 模式，生产环境应保证中间件高可用。
4. **司机端"我的"页**：已新增退出登录，可后续扩展修改密码、消息通知等功能。

---

## 十、部署架构

```
docker-compose (docker/docker-compose.yml)
├── tdengine      时序数据库
├── postgres      业务数据库
├── redis         缓存
├── kafka         消息队列
├── backend       FastAPI (多阶段构建)
├── frontend      Vue3 + Nginx (端口 3000:80)
└── simulator     传感器数据模拟器
```

前端修改后需重新构建并重启容器：
```bash
docker compose -f docker/docker-compose.yml up -d --build frontend
```

---

*文档生成时间：2026-07-07*
*基于代码实际探查，非凭空设计。*
