# 智能冷链监控平台 · 使用过程与角色功能说明

> 本文档基于项目真实代码梳理（前端 24 个页面 + 后端 18 个 API 模块），用于了解平台使用流程、移动端现状与待补功能、各角色权限与功能。
> 最后更新：2026-07-07

---

## 一、平台整体定位

智能冷链监控平台是一套覆盖「订单 → 调度 → 运输 → 入库/出库质检 → 全程溯源」的冷链全链路管理系统，包含：

- **PC 管理后台**（admin / warehouse 使用，17 个功能页）
- **司机移动端**（driver，MobileApp.vue，6 个 Tab）
- **客户移动端**（customer，CustomerOrders.vue，下单/追踪/签收）
- **公开溯源页**（消费者扫码查询，无需登录）

技术栈：Vue3 + FastAPI + TDengine(时序) + PostgreSQL(业务) + Redis(缓存) + Kafka(消息) + WebSocket(实时推送)。

---

## 二、登录与角色体系

### 账号（密码统一 `123456`，开发模式）

| 用户名 | 角色 | 进入页面 | 说明 |
|---|---|---|---|
| `admin` | 管理员/调度员 | `/dashboard` | 拥有全部 PC 功能 |
| `manager01` | 经理 | `/boss` | 经营分析看板（角色映射为 manager，实际等同 admin 权限） |
| `warehouse01` | 仓管维修 | `/warehouse` | 仓库工作台 + 大部分管理功能 |
| `driver01` | 司机 | `/driver-app` | 司机移动端工作台 |
| `customer01` | 客户 | `/customer-app` | 客户移动端下单/追踪 |

> ⚠️ 注意：登录页通过**用户名**区分角色（无角色下拉框）。`manager01` 映射到 `/boss` 但后端无 `manager` 角色（只有 `boss` 且 role=admin），存在轻微不一致，但不影响使用。

### 权限隔离方式
- **路由守卫**（`router/index.ts`）：未登录→登录页；driver/customer 只能访问自己的移动端路径；admin 拥有所有页面；warehouse 可访问标记了 `warehouse` 或 `admin` 的页面。
- **导航菜单过滤**（`MainLayout.vue`）：PC 端按角色隐藏无权限菜单项。
- **后端接口**：所有 `/driver/*` 接口用 `require_role("driver", "admin")` 保护。

---

## 三、各角色使用过程与功能清单

### 1. 司机（driver）→ 移动端 `/driver-app`

**入口**：手机浏览器访问 → 自动跳转 `/mobile`（或手动 `/driver-app`）

**当前 6 个 Tab 与功能**：

| Tab | 功能 | 具体操作 |
|---|---|---|
| 📊 监控 | 车厢实时状态 | 温度/湿度/冷机状态/车门状态/多温区监控/位置（需先绑定车辆） |
| 🗺 进度 | 配送进度 | 车辆信息、路线站点进度条、时效（准时/延误）、当前运单温控 |
| 🚨 预警 | 异常预警 | 查看预警（全部/紧急/严重/警告筛选）、点击"处理"→选处置方式+备注 |
| 📸 凭证 | 拍照上传 | 普通拍照 / 温度记录纸 AI 识别 / 上传记录列表 |
| 📋 运单 | 运单管理 | 全部/配送中/已送达/已完成 筛选、点击查看详情弹窗 |
| 👤 我的 | 个人中心 | 用户信息卡片 + 退出登录 |

**司机能用的后端接口**：dashboard、door-status、delivery-progress、alerts、alerts/{id}/handle、upload-record、upload-history、waybills、waybills/{id}、tracking（共 9 个，均受 driver/admin 角色保护）。

---

### 2. 客户（customer）→ 移动端 `/customer-app`

**入口**：手机浏览器访问 `/customer-app`

**功能**：
- 消费概览卡片（全部订单/配送中/已完成/消费总额）
- 智能温区推荐（下单时）
- 我的订单：状态过滤、订单列表（路线/温区/价格/温度追踪条）、订单详情
- 下单、追踪、签收

**客户还能通过**：`/public/trace/:traceCode`（扫码溯源，无需登录）查询全程温控曲线、追溯链、异常记录、区块链存证。

---

### 3. 管理员（admin）→ PC 后台 `/dashboard`

**拥有全部 17 个 PC 功能页**：

| 分组 | 功能页 | 用途 |
|---|---|---|
| 监控中心 | 全局态势图 | KPI 总览（在线率/达标率/告警/温湿度） |
| | 运营总览(/boss) | 经理 KPI 决策看板 |
| | 仓管维修工作台 | 仓库工作概览/订单审核/库存/质检/拍照审核/故障预测 |
| | 车辆追踪 | GPS 定位与轨迹回放 |
| | 设备监控 | 车辆/冷库传感器卡片 |
| | 温度趋势 | 历史曲线与预测 |
| | 告警中心 | 三级预警确认/解决/启动预案 |
| AI 智能 | 路径规划 | 配送路径优化 |
| | 多温区调度 | 车厢温区智能分配 |
| | 故障预测 | 冷机预测性维护 |
| | 品质评估 | 生鲜 AI 视觉质检（入库/出库） |
| 运营管理 | 告警规则 | 阈值与规则配置 |
| | 电子围栏 | 地理围栏与越界预警 |
| | 资源调度 | 冷链资源智能分配 |
| | 冷链追溯 | 全链路追溯与溯源 |
| | 客户查询 | 客户温控查询服务 |
| | 订单管理 | 创建运单/调度分配/司机消息 |

---

### 4. 仓管维修（warehouse）→ PC 后台 `/warehouse`

**可用功能**：除 `/boss`、`/monitor`、`/rules`、`/geofence` 外的所有 PC 页（即 dashboard、warehouse、tracking、temperature、alerts、dispatch、maintenance、quality、resources、traceability、customer、manager-orders）。

**仓库工作台核心 Tab**：
- 🏠 工作概览（库存/临期/审核通过 KPI）
- 📋 订单审核（审核司机上报的运单）
- 📦 库存管理（入库/出库/库存查询）
- 🤖 品质质检（AI 视觉评估）
- 📷 拍照审核（审核司机上传的凭证照片）
- 🔧 故障预测（冷机健康度）

---

## 四、移动端还缺什么功能（对比 PC 端）

移动端目前只有**司机工作台（MobileApp.vue）** 和 **客户下单页（CustomerOrders.vue）**。以下 PC 端已有但移动端**缺失**的功能：

| 缺失功能 | PC 对应页 | 移动端现状 | 建议优先级 |
|---|---|---|---|
| 生鲜品质 AI 评估 | QualityAssessment.vue | ❌ 无入口 | 🔴 高（司机可在现场拍照评估） |
| 全程冷链追溯 | Traceability.vue | ⚠️ 仅公开扫码页，无司机/仓管内嵌入口 | 🟡 中 |
| 温度历史趋势 | TemperatureTrend.vue | ❌ 只有实时数字 | 🟡 中 |
| 电子围栏 | GeofenceManager.vue | ❌ 无 | 🟢 低（管理类） |
| 车辆追踪地图 | VehicleTracking.vue | ⚠️ 有基础位置，无地图轨迹 | 🟡 中 |
| 多温区调度 | MultiZoneDispatch.vue | ❌ 无 | 🟢 低（调度类） |
| 冷机故障预测 | MaintenancePredict.vue | ❌ 无（仓管 PC 有） | 🟡 中 |
| 告警规则配置 | AlertRules.vue | ❌ 无（管理类） | 🟢 低 |
| 大屏/经营看板 | Dashboard/BossDashboard | ❌ 无 | 🟢 低 |
| 仓库审核闭环 | WarehouseDashboard 拍照审核 | 司机上传后看不到审核状态详情 | 🟡 中 |

### 移动端已知问题 / 待优化
1. **温度记录纸 AI 识别是 Mock**：`recognizeTemperaturePaper()` 返回写死假数据，非真实后端识别（前端注释已标注"后端接口就绪后替换"）。
2. **角色无前端差异化**：MobileApp 对所有角色显示相同内容，角色只影响"能否进入"（路由层），不影响"看到什么"。
3. **实时数据用 HTTP 轮询**（10 秒一次），未接入后端已提供的 WebSocket 通道。
4. **登录角色不一致**：`manager01` 映射 `/boss` 但后端无 manager 角色。
5. **客户移动端独立**：`/customer-app` 与司机端完全分开，客户看不到温控曲线/追溯详情（需到 PC 或公开溯源页）。

---

## 五、典型使用流程示例

### 场景 A：司机一天的工作
```
1. 手机登录（driver01 / 123456）
2. 【监控】查看车厢温度/湿度/冷机/车门是否正常
3. 【进度】查看今日配送路线、时效、当前运单
4. 运输中温度异常 → 【预警】收到告警 → 点"处理" → 选"调整温度" + 备注
5. 到站点交接 → 【凭证】拍温度记录纸 → AI 识别温度是否合格 → 确认上传
6. 【运单】查看运单状态变化（配送中→已送达）
7. 【我的】→ 退出登录
```

### 场景 B：仓管收货质检
```
1. PC 登录（warehouse01 / 123456）→ 进仓库工作台
2. 【品质质检】AI 评估司机拍的生鲜图片 → 给出品质评分/等级
3. 【拍照审核】审核司机上传的凭证照片（通过/驳回）
4. 【订单审核】审核待调度运单
5. 【故障预测】查看冷机健康度，安排维修
```

### 场景 C：客户查货
```
方式1：手机登录（customer01）→ 我的订单 → 查看温控追踪条
方式2：扫码 → /public/trace/xxx → 查看全程温度曲线 + 追溯链 + 区块链存证
```

### 场景 D：管理员全局调度
```
1. PC 登录（admin）→ 全局态势图
2. 【订单管理】创建运单 → 分配给司机
3. 【告警中心】处理紧急预警 → 启动应急预案
4. 【资源调度】智能分配车辆/冷库
5. 【多温区调度】规划车厢温区
```

---

## 六、移动端功能待办清单（建议）

| 序号 | 功能 | 说明 | 优先级 |
|---|---|---|---|
| 1 | 品质评估移动化 | 司机现场拍生鲜→AI 评分（复用 QualityAssessment 逻辑） | 🔴 高 |
| 2 | 温度记录纸识别接后端 | 替换 Mock 为真实识别接口 | 🔴 高 |
| 3 | 运单内嵌追溯入口 | 司机/客户从运单详情跳转到追溯链 | 🟡 中 |
| 4 | 温度历史曲线 | 移动端查看历史温度趋势图 | 🟡 中 |
| 5 | 车辆地图轨迹 | 集成地图 SDK 显示实时位置与轨迹 | 🟡 中 |
| 6 | 角色差异化首页 | 不同角色移动端显示不同 Tab/内容 | 🟡 中 |
| 7 | WebSocket 实时推送 | 替换 HTTP 轮询，降低延迟与服务器压力 | 🟢 低 |
| 8 | 仓库审核状态回显 | 司机端显示照片审核结果（通过/驳回） | 🟡 中 |

---

## 七、部署说明（Docker）

前端通过多阶段构建部署，修改源码后必须重新构建容器：

```bash
cd cold-chain-monitoring
docker compose -f docker/docker-compose.yml up -d --build frontend
```

验证容器内产物：
```bash
docker exec coldchain-frontend grep '关键词' /usr/share/nginx/html/assets/xxx.js
```

> ⚠️ 本地 `vite build` 的 `dist/` 不会自动同步到容器，必须重新 `build frontend` 容器镜像。

---

*本文档基于代码静态分析生成，实际功能以部署环境运行为准。*
