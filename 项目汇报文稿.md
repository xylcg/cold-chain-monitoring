# 基于深度学习的智能冷链物流监控平台

---

## 小组成员及分工

| 姓名 | 角色 | 负责功能模块 |
|------|------|-------------|
| （成员1） | 组长/全栈 | 系统架构设计、Docker 容器化部署、Kafka 消息队列集成、KPI 仪表盘、设备监控页面 |
| （成员2） | 后端开发 | 传感器数据采集、温度异常 LSTM 检测、温度趋势预测、告警引擎、电子围栏管理 |
| （成员3） | 后端开发 | 路径智能规划、多温区车厢调度、生鲜品质 AI 评估、冷链追溯链、客户查询服务 |
| （成员4） | 前端开发 | 前端页面开发（告警中心、车辆追踪、路径规划、品质评估、资源管理、移动端监控） |

> 注：以上为示例分工，请根据实际情况填写姓名和具体完成的功能模块。

---

## 一、项目总体业务描述

本项目构建了一套覆盖 **"产地预冷 → 冷藏运输 → 冷仓存储 → 末端配送"** 全链路的智能冷链物流监控平台。平台融合了物联网感知、深度学习、消息队列流处理和边缘计算技术，实现对冷链环境的实时感知、智能预测和主动调控。

**核心业务流程：**

1. **多传感器数据采集**：100辆冷藏车和10座冷库通过物联网设备上报温度、湿度、GPS、车门状态、冷机功率、振动、电池电量等传感器数据
2. **流式数据处理**：Kafka 消息队列缓冲传感器数据流，后端消费者实时消费并写入 TDengine 时序数据库
3. **AI 智能分析**：LSTM 自编码器检测温度异常（延迟 ≤10秒），深度学习模型预测未来30分钟温度趋势并提供越限风险等级
4. **智能预警处置**：三级预警（一般/严重/紧急）自动分级，通过 WebSocket 实时推送至 Web 管理后台
5. **业务智能调度**：多目标路径优化、多温区货物组合调度、冷机故障预测性维护、冷链资源利用率优化
6. **全链路追溯**：从产地到消费者的全程温湿度记录，SHA256哈希链存证防篡改，一键生成追溯报告

**技术架构：** FastAPI (Python) + Vue3 (TypeScript) + Kafka + Redis + PostgreSQL + TDengine + PyTorch + Docker Compose

**目标用户：** 配送人员（移动端）、运营管理人员（Web后台）、区域经理/维修团队、货主/消费者（查询端）

---

## 二、具体功能列表

### 2.1 MVP 第一阶段功能 (6个模块)

| 序号 | 功能模块 | 功能描述 | 关键能力 |
|:----:|----------|----------|----------|
| 1 | **多传感器数据采集** | 支持 100+ 冷链设备同时上报 16 类传感器数据（温湿度、GPS、车门、冷机、振动、电池、信号强度等），数据经 Kafka 流处理后写入 TDengine 时序数据库 | 并发 ≥1000条/秒，在线设备 ≥3000个 |
| 2 | **温度异常实时检测** | 基于 LSTM 自编码器的无监督异常检测，实时分析温湿度数据流，自动识别温度越限、冷机故障、车门异常开启等，端到端延迟 ≤10秒 | LSTM 深度学习模型，3类异常自动识别 |
| 3 | **温控趋势智能预测** | 基于历史传感器数据的深度学习预测模型，输出未来30分钟温度曲线、置信区间上下界、越限风险等级（0-3级），支持预警决策 | 30分钟预测窗口，含置信区间 |
| 4 | **移动端冷链监控（基础版）** | 配送员通过移动端页面实时查看所属车辆温湿度/车门状态，接收告警推送，拍照上传温度记录纸凭证 | WebSocket实时推送，图片上传 |
| 5 | **运营管理后台** | KPI 仪表盘（温控达标率、设备在线率、能耗统计等）、全局设备态势地图、设备台账管理 | 多维度KPI实时统计 |
| 6 | **智能预警与应急处置** | 三级预警自动分级（一般/严重/紧急），告警确认与处置流程，告警规则 CRUD 管理，通知渠道配置 | 3级预警，自动分级，处置闭环 |

### 2.2 后期迭代功能（8个模块）

| 序号 | 功能模块 | 功能描述 | 关键能力 |
|:----:|----------|----------|----------|
| 7 | **冷机故障预测性维护** | 基于 Weibull 分布的剩余寿命预测 + 梯度提升树故障概率评估，自动生成预防性维护计划，记录维护历史与成本统计 | Weibull + GBDT 双模型预测 |
| 8 | **冷链路径智能规划** | 多目标优化算法（时效+能耗+成本），支持温敏等级货物优先级排序，多条路径方案对比分析，全国城市坐标模拟 | 多目标帕累托优化 |
| 9 | **多温区车厢智能调度** | 支持冷冻(-22℃~-15℃)、冷藏(0℃~4℃)、恒温(15℃~25℃)三温区订单管理，贪心算法实现货物组合优化与车辆自动分配，最小化车辆使用数 | 三温区组合优化，装载率 ≥85% |
| 10 | **生鲜品质 AI 评估** | 基于计算机视觉识别水果/蔬菜/肉类新鲜度等级，自动品质分级，剩余保鲜期预测，对接阿里云百炼 AI 大模型能力 | CV + AI大模型，多品类识别 |
| 11 | **冷链电子围栏管理** | 围栏区域 CRUD 管理（支持圆形/多边形），车辆进出围栏自动检测记录，装卸货区域温控衔接监控，围栏事件历史 | 实时进出检测，温控衔接 |
| 12 | **全程冷链追溯链** | 全链路温湿度时间轴记录，SHA256 哈希链存证防篡改，逐块验证追溯，一键生成下载追溯报告 | 区块链哈希链，防篡改验证 |
| 13 | **冷链资源智能调度** | 冷库库位利用率管理（冷冻/冷藏/常温三温区），冷藏车辆资源池，蓄冷板/冰排库存管理，24小时需求预测与自动分配 | 利用率可视化，智能分配 |
| 14 | **客户温控查询服务** | 货主/消费者通过运单号查询全链路温度记录，温度曲线可视化展示，生成并下载温度合格证明文件 | 运单查询，证明文件下载 |

### 2.3 基础设施功能

| 功能 | 描述 |
|------|------|
| **JWT 认证授权** | 基于角色的访问控制（管理员/配送员/经理），Token 过期自动刷新 |
| **WebSocket 实时推送** | 设备实时数据推送、告警通知推送、多客户端连接管理 |
| **GPS 车辆实时追踪** | 100辆冷藏车 GPS 实时位置显示，历史轨迹回放，全国城市坐标 | 
| **传感器数据模拟器** | 独立 Python 模拟器，生成 100辆车+10座冷库的真实传感器数据流，持续写入 Kafka |
| **文件上传服务** | 支持温度记录纸拍照上传，图片存储与关联 |
| **Docker 容器化部署** | docker-compose 一键启动全部 8 个服务（后端/前端/Kafka/Redis/PostgreSQL/TDengine/ZooKeeper/模拟器） |

---

## 三、可执行的项目源码

### 3.1 项目目录结构

```
冷链物流监控平台/
├── backend/                        # 后端服务 (FastAPI + Python)
│   ├── app/
│   │   ├── api/                    # 17个 API 路由模块
│   │   │   ├── sensors.py          #   传感器数据采集
│   │   │   ├── temperature.py      #   温度异常检测与预测
│   │   │   ├── alerts.py           #   告警管理与规则
│   │   │   ├── dashboard.py        #   KPI 仪表盘
│   │   │   ├── auth.py             #   JWT 认证
│   │   │   ├── vehicles.py         #   车辆追踪
│   │   │   ├── websocket.py        #   WebSocket 实时推送
│   │   │   ├── geofence.py         #   电子围栏管理
│   │   │   ├── traceability.py     #   冷链追溯链
│   │   │   ├── customer.py         #   客户查询服务
│   │   │   ├── maintenance.py      #   故障预测性维护
│   │   │   ├── route_planning.py   #   路径智能规划
│   │   │   ├── dispatch.py         #   多温区车厢调度
│   │   │   ├── quality.py          #   生鲜品质AI评估
│   │   │   ├── resources.py        #   冷链资源调度
│   │   │   └── upload.py           #   文件上传
│   │   ├── core/                   # 核心配置（安全/数据库/CORS）
│   │   ├── services/               # 业务服务层
│   │   │   ├── kafka_service.py    #   Kafka 生产者/消费者
│   │   │   ├── redis_service.py    #   Redis 缓存服务
│   │   │   ├── tdengine_service.py #   TDengine 时序数据库
│   │   │   ├── alert_engine.py     #   告警分析引擎
│   │   │   ├── model_service.py    #   深度学习模型服务
│   │   │   └── websocket_manager.py#   WebSocket 连接管理
│   │   └── main.py                 # FastAPI 应用入口
│   ├── requirements.txt            # Python 依赖清单
│   └── Dockerfile                  # 后端 Docker 镜像
│
├── frontend/                       # 前端 (Vue3 + TypeScript + Element Plus)
│   ├── src/
│   │   ├── views/                  # 16个页面组件
│   │   ├── api/index.ts            # API 接口封装
│   │   ├── router/index.ts         # 路由配置
│   │   ├── stores/                 # Pinia 状态管理
│   │   └── styles/global.css       # 全局样式（极光操作台主题）
│   ├── package.json                # 前端依赖清单
│   └── Dockerfile                  # 前端 Docker 镜像
│
├── models/                         # 深度学习模型 (PyTorch)
│   ├── lstm_anomaly_detector.py    #   LSTM 温度异常检测
│   └── temperature_predictor.py    #   温度趋势预测
│
├── simulator/                      # 传感器数据模拟器
│   └── sensor_simulator.py         # 100辆车+10座冷库数据生成
│
├── docker/                         # Docker 部署配置
│   ├── init-postgres.sql           # PostgreSQL 初始化脚本
│   └── nginx.conf                  # Nginx 反向代理配置
│
├── docker-compose.yml              # 一键部署编排文件
├── requirements.txt                # 项目总依赖
└── README.md                       # 项目说明文档
```

### 3.2 核心技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| **前端框架** | Vue 3 + TypeScript | SPA 单页应用，Element Plus UI 组件库 |
| **可视化** | ECharts + Leaflet | 数据图表 + GPS 地图追踪 |
| **后端框架** | FastAPI (Python 3.10+) | 异步 REST API + WebSocket |
| **关系数据库** | PostgreSQL 15 | 业务数据（用户/设备/订单/告警规则） |
| **时序数据库** | TDengine 3.0 | 传感器时序数据（温湿度/GPS） |
| **缓存** | Redis 7 | 实时数据缓存、会话管理 |
| **消息队列** | Apache Kafka | 传感器数据流缓冲与解耦 |
| **AI 框架** | PyTorch + scikit-learn | LSTM 异常检测、GBDT 故障预测 |
| **容器化** | Docker + Docker Compose | 8个服务容器编排，一键部署 |

---

## 四、项目启动和依赖说明

### 4.1 环境要求

| 软件 | 最低版本 | 用途 |
|------|----------|------|
| Docker | 24.0+ | 容器运行时 |
| Docker Compose | v2.20+ | 多容器编排 |
| Python | 3.10+ | 后端开发环境（可选，通过 Docker 部署无需本地安装） |
| Node.js | 18+ | 前端开发环境（可选，通过 Docker 部署无需本地安装） |
| 内存 | ≥ 8GB | 运行全部 8 个容器服务 |
| 磁盘 | ≥ 10GB | 包含 Docker 镜像和数据卷 |

### 4.2 Docker 一键部署（推荐）

```bash
# 1. 克隆项目
cd 冷链物流监控平台/

# 2. 启动全部服务（首次需拉取镜像约5-10分钟）
docker compose up -d

# 3. 等待服务就绪后访问
# Web 管理后台: http://localhost:3000
# 测试账号: admin / 123456
```

首次启动会自动完成以下工作：拉取基础镜像（TDengine/PostgreSQL/Redis/Kafka/ZooKeeper）、构建前后端镜像、初始化数据库表结构、启动传感器数据模拟器。

### 4.3 本地开发环境

**后端开发：**

```bash
cd backend/
pip install -r requirements.txt

# 启动 Kafka + Redis + PostgreSQL + TDengine（通过 Docker）
docker compose up -d kafka redis postgres taos zookeeper

# 启动后端开发服务器
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

**前端开发：**

```bash
cd frontend/
npm install

# 启动前端开发服务器（热重载）
npm run dev
# 访问 http://localhost:3000
```

### 4.4 后端依赖清单 (requirements.txt)

```
fastapi>=0.104.0              # Web 框架
uvicorn[standard]>=0.24.0     # ASGI 服务器
pydantic>=2.0                 # 数据校验
pydantic-settings>=2.0        # 配置管理
python-jose[cryptography]>=3.3.0  # JWT 认证
passlib[bcrypt]>=1.7.4        # 密码加密
python-multipart>=0.0.6       # 文件上传
httpx>=0.25.0                 # HTTP 客户端
sqlalchemy>=2.0               # ORM
asyncpg>=0.29.0               # PostgreSQL 异步驱动
psycopg2-binary>=2.9.0        # PostgreSQL 驱动
redis>=5.0                    # Redis 客户端
kafka-python>=2.0             # Kafka 生产者
aiokafka>=0.10.0              # Kafka 异步消费者
taospy>=2.7.0                 # TDengine 客户端
websockets>=12.0              # WebSocket 支持
loguru>=0.7.0                 # 日志
pillow>=9.0.0                 # 图像处理
numpy>=1.24.0                 # 数值计算
dashscope>=1.0.0              # 阿里云百炼 AI（品质评估）
python-dotenv>=1.0.0          # 环境变量
```

### 4.5 前端依赖清单 (package.json)

```
vue: ^3.4.0                   # 渐进式前端框架
vue-router: ^4.2.5            # 单页路由
pinia: ^2.1.7                 # 状态管理
element-plus: ^2.4.3          # UI 组件库
echarts: ^5.4.3               # 数据可视化图表
leaflet: ^1.9.4               # 地图可视化
axios: ^1.6.2                 # HTTP 请求
dayjs: ^1.11.10               # 日期处理
typescript: ^5.3.2            # 类型系统
vite: ^5.0.10                 # 构建工具
```

### 4.6 测试账号

| 用户名 | 密码 | 角色 | 可访问模块 |
|--------|------|------|------------|
| `admin` | `123456` | 系统管理员 | 全部功能 |
| `driver01` | `123456` | 配送员 | 设备监控、移动端、告警查看 |
| `manager01` | `123456` | 区域经理 | 仪表盘、车辆追踪、告警管理、报表 |

### 4.7 Docker 服务列表

| 服务名 | 容器名 | 端口 | 说明 |
|--------|--------|------|------|
| frontend | coldchain-frontend | 3000:80 | Vue3 + Nginx Web 前端 |
| backend | coldchain-backend | 8000 (内部) | FastAPI 后端服务 |
| kafka | coldchain-kafka | 9092 (内部) | 消息队列 |
| zookeeper | coldchain-zookeeper | 2181 (内部) | Kafka 依赖 |
| redis | coldchain-redis | 6379 (内部) | 缓存服务 |
| postgres | coldchain-postgres | 5432 (内部) | 业务数据库 |
| taos | coldchain-taos | 6030,6041 | 时序数据库 |
| simulator | coldchain-simulator | - | 数据模拟器 |

---

> 本文档仅用于项目功能说明与汇报展示，非毕业设计论文格式。
