# 冷链 IoT 监控平台 · 典型使用流程场景示例

> 本文档是《平台使用过程与角色功能说明》《业务流程方案》的**场景补充篇**。
> 目标：用**多角色、多情境、正常+异常+应急**的真实操作流，覆盖平台使用的各个方面。
> 所有步骤均基于项目真实代码与已部署能力（含 2026-07-07 补齐的移动端能力）。
> 最后更新：2026-07-07

---

## 〇、阅读约定

- 账号密码（开发模式）：所有账号密码统一 `123456`
  - `admin` / `manager01`（管理员、经理看板）· `warehouse01`（仓管）· `driver01`（司机）· `customer01`（客户）
- 移动端访问：手机浏览器打开 `http://<部署域名或IP>:3000`
  - 司机端自动进入 `/driver-app`；客户端进入 `/customer-app`；公开溯源页 `/public/trace/:code`（无需登录）
- 真实识别开关：温度记录纸 AI 识别、生鲜品质评估等视觉能力需要环境变量 `ZHIPU_API_KEY`；未配置时自动降级为规则模拟（结果标注"模拟"），演示闭环不中断。
- 本文"点击"= 移动端 Tab/按钮；"进入页面"= PC 端左侧菜单。

---

## 一、司机（driver）角色场景

### 场景 A1：司机一天的标准工作流（正常）
```
1. 手机登录 driver01 / 123456 → 进入司机工作台（6 Tab）
2. 【监控】查看车厢温湿度、冷机状态、车门状态、多温区、车辆位置（需先绑定车辆）
3. 【进度】查看今日配送路线站点进度条、时效（准时/延误）、当前运单温控
4. 到站交接 → 【凭证】普通拍照留存交接照片 → 确认上传
5. 【凭证】→ 拍"温度记录纸" → 点【AI识别】→ 后端 OCR+视觉识别各时间点温度
   · 识别结果：合格点/超标点、整体判定 pass/fail、处置建议
   · 若未配 ZHIPU_API_KEY，显示模拟结果（标注 is_simulated）
6. 【运单】查看运单状态（配送中 → 已送达），点详情弹窗看【🌡温度曲线】
   · 曲线含合规阈值带、超标点标红
7. 收车 → 【我的】→ 退出登录
```

### 场景 A2：运输途中温度异常 + 预警处置（异常闭环）
```
1. 【监控】发现车厢温度从 4℃ 爬升到 9℃（超阈值 8℃）
2. 系统推送预警 → 【预警】Tab 出现红色"严重"告警
   · 可按 全部/紧急/严重/警告 筛选
3. 点告警 → "处理" → 选处置方式（调整温度 / 联系调度 / 紧急停车）
   · 填写备注："冷机设定偏低，已上调至 5℃"
4. 处置后状态变为"已处理"，预警从待处理列表移除
5. 回到【监控】确认温度回落至 5℃ 达标
6. （PC 侧）管理员在【告警中心】也能看到该告警及司机处置记录
```

### 场景 A3：到仓后拍照凭证 + 等待仓管审核（协同）
```
1. 司机在【凭证】上传到货照片与温度记录纸
2. 仓管在 PC【拍照审核】看到待审照片 → 通过 / 驳回
3. 司机当前端"上传记录列表"显示该凭证（后续可扩展显示审核状态）
```

---

## 二、客户（customer）角色场景

### 场景 B1：客户下单 → 追踪 → 签收（标准消费链路）
```
1. 手机登录 customer01 / 123456 → 进入客户移动端
2. 消费概览卡片：全部订单 / 配送中 / 已完成 / 消费总额
3. 点【＋下单】→ 选温区（系统智能温区推荐：冷冻/冷藏/常温）
   · 填货物、路线、温控要求 → 提交
4. 【我的订单】看订单列表（路线/温区/价格/温控追踪条）
5. 点订单 → 进入【追踪】弹窗：
   · 🌡 全程温度曲线（canvas 绘制，含阈值带）
   · 🔗 区块链存证卡片（区块号/哈希/Merkle根/存证时间）
   · 配送进度时间线
6. 货到 → 点【签收】完成
```
> ✅ **下单接口已补齐（2026-07-07）**：后端 `customer.py` 已新增 `POST /customer/create-order`
> 与配套的 `GET /customer/my-orders-new`（内存存储，演示用）。`submitOrder()` 调 `customerAPI.createOrder`
> 提交后返回 `{ status:'ok', order:{ order_id, trace_code, price, status:'pending', ... } }`，
> 并自动刷新订单列表。已用 customer01 实测：下单生成 `WB202607070001` / 溯源码 `CC0707963` / 运费 670.0，
> `my-orders-new` 正确返回该客户名下订单。B1 场景现已**完整端到端可用**。

### 场景 B2：客户扫码 / 输码查货（场景 C 移动化，2026-07-07 新增）
```
方式一（扫码）：
1. 客户移动端底部导航点【🔍查货】
2. 点【📷扫码】→ 调用摄像头对准运单/溯源码二维码
   · 自动识别失败则提示手动输入
3. 识别成功 → 自动填入运单号/溯源码 → 点【查询】

方式二（手动输入）：
1. 点【🔍查货】→ 输入运单号（如 WB20240101001）或溯源码（如 CC1001）
2. 点【查询】→ 调 /customer/mini/query
3. 结果展示：
   · 货品名 + 合规状态（✅全程合规 / ⚠️温度异常）
   · 运单号 / 溯源码 / 路线 / 温控要求 / 当前温度 / 异常次数
   · 各节点进度步骤（下单→调度→运输→入库→签收）
```

### 场景 B3：客户在 PC 端查看详细追溯（深度溯源）
```
1. 客户登录后进入【客户查询】页（或管理员代查）
2. 输入运单号 → 查看全链路温控曲线、各节点时间、合规状态、异常记录
3. 点【复制溯源码】→ 把溯源码发给下游消费者，供其扫码查证
```

---

## 三、消费者（公开，无需登录）场景

### 场景 C1：消费者扫码溯源（信任闭环）
```
1. 收到货 → 扫描包装上的溯源二维码
   · 二维码内容即 /public/trace/:traceCode（如 /public/trace/CC1001）
2. 打开公开溯源页（无需登录）
3. 查看：
   · 全程温度曲线（达标区间高亮）
   · 各节点时间线（产地预冷→运输→入库→配送→签收）
   · 异常记录（如有超标，明确标出时间点与温度）
   · 🔗 区块链存证校验结果（已上链/哈希/区块号）
4. 信任确认：全程合规 → 放心收货；有异常 → 可凭溯源码向商家维权
```

---

## 四、仓管（warehouse）角色场景

### 场景 D1：入库质检 + 拍照审核（标准收货）
```
1. PC 登录 warehouse01 / 123456 → 进【仓库工作台】
2. 【订单审核】查看待调度/待入库运单 → 审核通过
3. 【品质质检】切到"入库质检" → 上传/拍司机送达的生鲜图片
   · AI 评估品质等级（A/B/C/D）+ 保鲜期建议 + 放行决策
   · 支持品类：水果/蔬菜/肉类/海鲜/乳制品/豆制品/蛋类/医药/花卉
4. 【拍照审核】审核司机在移动端上传的凭证照片 → 通过 / 驳回
5. 【库存管理】执行入库登记 → 库存更新
```

### 场景 D2：出库复核 + 冷机故障预警（异常预防）
```
1. 【品质质检】切到"出库复核" → 重新评估出库品质（切换自动清空上次结果）
2. 【故障预测】查看冷机健康度评分
   · 若评分偏低 → 安排维修，避免运输途中故障
3. 【库存管理】出库登记 → 生成出库记录进入追溯链
```

---

## 五、管理员（admin）角色场景

### 场景 E1：全局调度一天（运营中枢）
```
1. PC 登录 admin / 123456 → 【全局态势图】
   · KPI：设备在线率 / 温度达标率 / 活跃告警数 / 温湿度概览
2. 【订单管理】创建运单 → 分配司机 → 发司机消息
3. 【多温区调度】按货物温区要求规划车厢温区（冷冻/冷藏/常温）
4. 【资源调度】分配冷库仓位、冷藏车辆、冷板
5. 【路径规划】为司机生成最优配送路线（支持途中重规划）
6. 【电子围栏】划定合规区域，车辆越界/超时触发围栏告警
```

### 场景 E2：紧急告警应急处置（应急闭环）
```
1. 【告警中心】收到"严重/紧急"告警（Kafka 推送）
2. 点告警 → 【确认】→ 查看处置预案
3. 启动【应急处置预案】→ 按步骤执行（通知司机/切换备用车/上报）
4. 处置完成 → 【解决】→ 告警归档
5. 同步在【告警规则】复核阈值是否合理，必要时调整
```

### 场景 E3：经营分析（经理视角）
```
1. 登录 manager01 / 123456 → 自动进入【经营分析看板 /boss】
2. 查看：经营财务分析、司机绩效、客户分析、资源利用率
3. 据此决策：运力调配、客户分级、成本优化
```

---

## 六、跨角色协同场景（端到端全链路）

### 场景 F1：一票货的完整体命周期（覆盖全部环节）
```
[客户] customer01 下单（冷冻三文鱼，温控 -18~6℃）
  → [管理员] admin 创建运单、多温区派车、分配冷库与车辆、规划路径
    → [司机] driver01 接单 → 运输中监控温湿度
      · 温度异常 → 预警处置闭环（场景 A2）
      · 到站 → 拍温度记录纸 AI 识别（场景 A1）
    → [仓管] warehouse01 入库质检（AI 品质评估）+ 拍照审核（场景 D1）
      → 出库复核 + 冷机故障预测（场景 D2）
    → [司机] 末端配送 → 客户签收（场景 B1）
      → [系统] 自动写入追溯链 + 区块链存证
    → [消费者] 扫码溯源验证（场景 C1）
    → [客户] 移动端查货/追踪查看温度曲线与存证（场景 B2/B1）
```

### 场景 F2：温度超标引发的信任危机处理（异常+溯源+维权）
```
1. 【消费者】扫码发现温度曲线有 2 处超标（红色点）→ 质疑货品
2. 【消费者】凭溯源码联系【客户】customer01 维权
3. 【客户】移动端【查货】输入溯源码 → 看到"⚠️温度异常" + 异常次数
4. 【客户】PC【客户查询】导出温控报告（含证明编号 RPT-xxx）
5. 【管理员】在【追溯】页核对区块链存证（不可篡改）→ 确认责任节点
6. 【管理员】按追溯链定位到"运输段超标" → 追责司机/调度，优化冷机参数
```

### 场景 F3：无网/无 Key 的降级演示闭环（健壮性）
```
前提：未配置 ZHIPU_API_KEY、TDengine/Redis 不可达
1. 【司机】温度记录纸识别 → 返回规则模拟结果（标注 is_simulated=true）
2. 【客户】温度曲线/区块链存证 → 后端 fallback 内存数据正常展示
3. 【消费者】公开溯源页 → 仍可读到模拟溯源链与存证
结论：任一中间件缺失，演示流程不中断，仅识别精度降级
```

---

## 七、场景覆盖矩阵（确保无死角）

| 维度 | 覆盖场景 | 角色 |
|------|----------|------|
| 日常监控 | A1 司机标准工作、E1 全局调度 | driver / admin |
| 异常处置 | A2 温度异常预警、E2 应急闭环、F2 超标维权 | driver / admin / customer / 消费者 |
| 质检入库 | D1 入库质检、D2 出库复核 | warehouse |
| 客户消费 | B1 下单追踪签收、B2 扫码查货 | customer |
| 公开信任 | C1 消费者扫码溯源 | 消费者（无登录） |
| 协同闭环 | F1 全链路生命周期、A3 凭证审核协同 | 全员 |
| 经营决策 | E3 经理看板 | manager |
| 健壮性降级 | F3 无网/无 Key 演示闭环 | 全员 |
| 移动端新能力 | A1 温度记录纸真识别+温度曲线、B1/B2 曲线+存证+查货 | driver / customer |

---

## 八、关键接口速查（场景对应）

| 场景动作 | 接口 | 端 |
|----------|------|----|
| 温度记录纸识别 | `POST /driver/recognize-temperature-paper` | 司机移动端 |
| 运单温度曲线 | `GET /customer/query/temperature-curve?waybill_id=` | 司机/客户移动端 |
| 客户查货 | `GET /customer/mini/query?code=` | 客户移动端 |
| 区块链存证 | `GET /customer/verify?waybill_id=` 或 `/traceability/blockchain/verify/:code` | 客户/PC/公开 |
| 公开溯源 | `GET /traceability/public/:traceCode` | 消费者（无登录） |
| 预警处置 | `POST /driver/alerts/{id}/handle` · `/alerts/handle` | 司机/PC |
| 品质评估 | `POST /quality/assess` · `/assess/image` | 仓管 PC |
| 全局调度 | `/dispatch/*` · `/resources/*` · `/routes/*` · `/geofence/*` | 管理员 PC |

---

## 九、功能可用性核验（基于代码实测，2026-07-07）

为避免"文档写得好看但跑不通"，以下按场景标注**当前部署环境真实可用性**：

| 场景 | 关键接口 | 后端状态 | 可用性 |
|------|----------|----------|--------|
| A1 司机监控/进度/凭证/运单 | `/driver/dashboard`、`/delivery-progress`、`/waybills`、`/recognize-temperature-paper` | ✅ 均存在 | ✅ 完整可用 |
| A1 温度记录纸识别 | `POST /driver/recognize-temperature-paper` | ✅ 存在（无 Key 降级模拟） | ✅ 可用（模拟精度） |
| A1 运单温度曲线 | `GET /customer/query/temperature-curve` | ✅ 存在 | ✅ 可用 |
| A2 预警处置 | `/driver/alerts`、`/alerts/{id}/handle` | ✅ 存在 | ✅ 可用 |
| A3 拍照凭证+仓管审核 | `/driver/upload-record`、`/warehouse` 拍照审核 | ✅ 存在 | ✅ 可用（审核状态回显待扩展） |
| B1 追踪/签收/曲线/存证 | `/customer/tracking`、`/customer/verify`、`/customer/query/temperature-curve` | ✅ 存在 | ✅ 第4–6步可用 |
| B1 下单 | `POST /customer/create-order` + `GET /customer/my-orders-new` | ✅ 已补齐（2026-07-07） | ✅ 完整可用 |
| B2 扫码查货 | `GET /customer/mini/query` | ✅ 存在 | ✅ 可用（手动输入稳定；摄像头扫码依赖 HTTPS+权限） |
| B3 PC 客户查询 | `/customer/query`、`/traceability` | ✅ 存在 | ✅ 可用 |
| C1 公开溯源 | `GET /traceability/public/:code` | ✅ 存在（traceability.py） | ✅ 可用（需真实 trace_code，如 CC1001） |
| D1/D2 仓管质检/审核/故障 | `/quality/assess`、`/warehouse`、`/maintenance` | ✅ 存在 | ✅ 可用 |
| E1/E2/E3 管理员调度/应急/看板 | `/dispatch/*`、`/resources/*`、`/routes/*`、`/geofence/*`、`/alerts/*`、`/boss` | ✅ 存在 | ✅ 可用 |
| F1/F2/F3 协同/维权/降级 | 综合上述 | 取决于子场景 | ✅ 除 B1 下单外均可用 |

**唯一缺口**：客户移动端"下单生成运单"（`/customer/create-order`）后端未实现。
**建议**：要么后端补齐该路由，要么 B1 改为"管理员预置运单 → 客户追踪签收"的演示路径。

---

*本文档与《平台使用过程与角色功能说明》《业务流程方案》配套使用，覆盖真实使用的各个方面。*
